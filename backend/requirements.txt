# Trasset — Backend requirements
# Python 3.11+  |  Django REST Framework  |  MySQL 8.x

# --- Core ---
Django==5.1.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.4.0
django-cors-headers==4.6.0
django-filter==24.3
django-environ==0.12.0
drf-spectacular==0.28.0

# --- Database ---
mysqlclient==2.2.7 ; platform_system != "Windows"
PyMySQL==1.1.1

# --- Security ---
argon2-cffi==23.1.0

# --- Media / files ---
Pillow==11.1.0
qrcode==8.0
openpyxl==3.1.5

# --- Async / scheduled jobs ---
celery==5.4.0
redis==5.2.1
django-celery-beat==2.7.0

# --- Server ---
gunicorn==23.0.0 ; platform_system != "Windows"
whitenoise==6.8.2
